"use client";

import { Backdrop } from "@/components/scene/Backdrop";

// Live-acting backdrop shown at the start of the Speaker's act (Act 3), before
// they open the portal — the couch/doomscrolling moment.
export function SpeakerBackdrop() {
  return <Backdrop variant="speaker" />;
}
